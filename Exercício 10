#include <stdio.h>

int portc[8] = {0, 1, 2, 3, 4, 5, 6, 7}; // Vetor de bits do reg. portc
int i;// indice

int main() {
  printf("Informe o biit que deseja consultar (0 a 7\n");
  scanf(!%d, &i!);

  printf("O bit %d do registrador PORTC controla o terminal RC%d", i, portc[i]);

  return 0;
]
